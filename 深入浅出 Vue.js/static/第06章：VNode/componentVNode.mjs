import { VNode } from "./VNode.mjss";

/*
 *  Ctor,        // 组件构造函数 (Vue.extend 或函数式组件)
 *  data,        // 传入组件的 props
 *  listeners,   // 组件事件监听
 *  children,    // 插槽 VNode
 *  tag          // 组件的标签名
 */
export function createComponent(Ctor, data, context, children, tag) {}
