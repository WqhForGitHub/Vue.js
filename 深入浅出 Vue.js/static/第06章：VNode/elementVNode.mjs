import { createEmptyVNode } from "./emptyNode.mjs";

/*
 *  context：当前组件的 Vue.js 实例
 *  tag：节点名称
 *  data：节点数据
 *  children：当前节点的子节点列表
 */
export default function createElement(
  context,
  tag,
  data,
  children,
  normalizationType,
  alwaysNormalize
) {}
